# ============================================================
#  忽略规则说明
#
#  Git 的重要机制：.gitignore 只对「未被跟踪」的文件生效。
#  如果某个文件已经被 git add 过，规则不会自动把它排除，
#  必须先执行：  git rm --cached <文件>    停止跟踪
#  想让当前规则立刻对全部文件重新生效：
#               git rm -r --cached .  &&  git add .
#
#  本作业要求提交「源文件工程包 + 可执行文件 .exe」，
#  所以 exe 需按需手动加入，方法见文末。
# ============================================================

# ---------- 只忽略构建输出目录 ----------
# 用 build/ 而不是 *.exe：规则更精准，
# 避免把 .vscode/ 等其他位置需要提交的文件误伤
build/

# GUI 版编译出来的可执行文件
# （源码 GUI/ 不受影响，只有 exe 被忽略；需要提交时用 git add -f）
GUI/book_gui.exe
GUI/app.res

# ---------- 目标文件 / 中间产物 ----------
*.o
*.obj
*.ilk
*.pdb

# ---------- 编辑器中转文件 ----------
*.tmp
*.swp
*.bak

# ---------- VS Code ----------
.vs/
*.user
*.suo

# ---------- 系统 ----------
Thumbs.db
Desktop.ini
desktop.ini

# ============================================================
#  交作业时如何把 exe 放进仓库（exe 被 build/ 规则忽略了）
#
#  1) 单独强制添加（推荐，只加可执行文件）：
#         git add -f build/main.exe
#         git commit -m "提交可执行文件"
#     -f 表示 force，可覆盖忽略规则。
#
#  2) 或者把 exe 复制到工程根目录，再改成先编译到根目录：
#         忽略规则只覆盖 build/，根目录的 exe 不受影响。
#
#  注意：提交的应是 Release 版 exe
#  （任务「C++: 编译工程 (Release)」，静态链接，不依赖 MSYS2 运行库）。
# ============================================================
