# ============================================================
# 注意：本作业要求提交「源文件工程包 + 可执行文件 .exe」，
# 所以 build/ 里的 exe 被下面的规则排除了，不会进 Git。
# 交作业打包时请手动把 build/ 里的 .exe 一起放进压缩包。
# ============================================================

# ---------- 构建产物（排除，避免仓库冗余）----------
build/
*.o
*.obj
*.ilk
*.pdb
*.exe

# ---------- 编辑器中转文件 ----------
*.tmp
*.swp
*.bak

# ---------- VS Code ----------
.vs/
*.user
*.suo

# ---------- 系统 ----------
Thumbs.db
Desktop.ini
desktop.ini
